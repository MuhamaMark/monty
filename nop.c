#include "monty.h"

/**
 * m_nop - no operation performed
 * @stack: double pointer to the head of the stack
 * @line_no: line number of current operation
 * Return: void
 */
void m_nop(stack_t **stack, unsigned int line_no)
{
	(void)stack;
	(void)line_no;
}
